# Wheel placement and operator audit — candidate, not canonical

This audit leaves the existing README and code sketch intact and does not change repository visibility.

## Source architecture versus wheel geometry
The README contains functions RSCS, phi_perspective, phi_motive, memory, FPVF, PS, MFPVF, curvature, I, M_phi, TPL, S, C_unified, delta_RSCS and R_emptyset. These are computational functions and expressions; the README does **not** establish an authoritative centre plus twelve uniquely positioned perimeter nodes, Foundation/Transformation/Integration groups, six reciprocal pairs, or a historically verified X/Y/Z arrangement. Do not assign those placements solely by the order of functions in the code.

The source's RSCS(n,state) returns a vector norm, explicitly marked placeholder. It is **not** the canonical twelve-node deviation RSCS. The source's memory() generates random data and R_emptyset() returns placeholder zero; neither establishes persistent memory or a validated collapse operator. The name "consciousness" is a conceptual model label, not evidence of subjective experience.

## Required placement work
Identify the governing centre definition and twelve distinct semantic roles from authoritative wheel material; retain code functions as implementation references rather than forcing each function into one perimeter position. Only then map group, axis, polarity, elemental classification, reciprocal pairing, centre dependency and recursive flow with source citations.

## Operator and admission status
Canonical RSCS, PTE, lag, SS, RFC and DRSC: **uncomputed**, because qualified deviation inputs, transformation paths, overhead, timing, fusion set and dependency-cover evidence are absent. Historical placement: pending; numerical qualification: pending; corpus uniqueness: unresolved; DRSC admission: pending. Status: candidate, admitted count 0. No runtime authorization.
